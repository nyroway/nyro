// Package schema defines the pure configuration contract shared by Nyro's
// telemetry configuration inputs, distribution plane, and runtime.
//
// Layer: 0 (foundation) — may import the Go standard library only. It must not
// import other internal packages or contain runtime exporter construction.
package schema
